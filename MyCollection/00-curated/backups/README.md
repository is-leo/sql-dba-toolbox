# Backup Triage

Use this folder for backup health, restore readiness, recovery point questions, and log-chain checks.

Start with `01-last-backups.sql` to confirm the last full, differential, and log backups per database.

Always confirm recovery model, backup location, retention, job status, disk space, and whether the database participates in AG, log shipping, or other HA/DR flows.
