# Configuration Triage

Use this folder for SQL Server instance and database configuration checks.

Start with `01-check-recommended-maxdop.sql` when troubleshooting parallelism, CPU pressure, or instance baseline configuration.

Configuration changes should include current value, recommended value, reason, risk, rollback command, and whether a restart is required.
